const flower=[
    "🌹", "🌺", "🌻", "🌼", "💐", "🏵️"
];
function createflowr(){
    const flowerdiv=document.createElement("div");
    flowerdiv.className="flower";
    const flowericon=flower[Math.floor(Math.random()*flower.length)];
    const index=Math.random()*window*wv
}